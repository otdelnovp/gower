function isVisible(elem) {	if (!$(elem).length) return false;	var docViewBottom = $(window).scrollTop() + $(window).height();	var elemBottom = $(elem).offset().top;	return ((elemBottom < docViewBottom));}function scroll() {	// move header	var scroll_top = $(window).scrollTop(), content_height = $('.content').height();	if (scroll_top >= content_height) {		$('body').addClass('scrolled');		if( !$('.header').hasClass("header_fixed") && $('.start').length ) {			$('.header').fadeOut('fast',function(){				$(this).addClass("header_fixed").fadeIn('fast');			});		}	} else {		$('body').removeClass('scrolled');		if( $('.header').hasClass("header_fixed") && $('.start').length ) {			$('.header').fadeOut('fast',function(){				$(this).removeClass("header_fixed").fadeIn('fast');			});		}	}	// page scrolled	if ($(window).scrollTop() >= $('.content').height()-10) {		$('body').addClass('scrolled');	} else {		$('body').removeClass('scrolled');	}	// visible footer	if (isVisible('.footer')) {		$('.scroll-top').addClass('bottom');	} else {		$('.scroll-top').removeClass('bottom');	}}$(document).ready(function(){		scroll(); $(window).bind('scroll', scroll);		// click and scroll content	$('.header__menu-link, .start-scroll, .scroll-top').click(function() {		idscroll = $(this).attr('href');		$.scrollTo(idscroll, 700);		return false;	});		// js scroll effects	new WOW().init();	});